import { useEffect, useState } from "react";

export const Dashboard: React.FC = () => {
  const [id, setId] = useState(0);

  useEffect(() => {}, []);

  return (
    <div>
      <div>dashboard</div>
    </div>
  );
};
