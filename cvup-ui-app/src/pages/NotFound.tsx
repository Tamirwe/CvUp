import { useEffect, useState } from "react";

export const NotFound: React.FC = () => {
  const [id, setId] = useState(0);

  useEffect(() => {}, []);

  return (
    <div>
      <div>NotFound</div>
    </div>
  );
};
