import { useEffect, useState } from "react";

export const Login: React.FC = () => {
  const [id, setId] = useState(0);

  useEffect(() => {}, []);

  return (
    <div>
      <div>Login</div>
    </div>
  );
};
