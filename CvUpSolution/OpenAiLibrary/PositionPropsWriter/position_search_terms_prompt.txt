You are a recruitment analyst.
Analyze the following job position and extract structured search terms for a CV matching system.

The position text may contain a formal job description and informal recruiter notes — extract from both.

Return ONLY a JSON object with these fields:
- lucene_keywords: { he: [...], en: [...] } — terms to use for keyword search against CVs, in both Hebrew and English. Include the job title, core hard skills, tools/technologies, certifications, and common synonyms/variations (e.g. abbreviations, alternate spellings, related role names). Prioritize terms that would actually appear in a CV.
- search_phrase: a short keyword-dense paragraph, written in both English and Hebrew, optimized for semantic vector search against CVs. Describe the ideal candidate's role, seniority, core skills, and domain in natural language (not a list) — this is embedded and matched by meaning, not exact terms.